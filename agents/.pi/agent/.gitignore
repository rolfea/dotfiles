# .pi/agent/.gitignore
# We use an allowlist strategy (ignore everything, then explicitly opt-in)
# to avoid syncing volatile runtime files (sessions, caches, tokens)
# back into the dotfiles repository.

*

!.gitignore
!AGENTS.md
!settings.json
!skills/
